import { Fragment } from "react";
import Auth from "./Auth";

const Account: React.FC = () => {
    return <Fragment>
        <h1>Account</h1>
        <Auth></Auth>
    </Fragment>;
}

export default Account;